# AGENTS.md

## 1. この文書の目的

このプロジェクトは、地域サッカー大会の2日間の日程、対戦表、審判割当てを生成・検証するプログラムを実装する。

本書は、このリポジトリで作業するAIエージェントおよび開発者が守るべき実装仕様である。大会ルールと実装上の制約を正本として扱い、README、画面、設定例、テストが本書と矛盾する場合は、ユーザーの最新指示を最優先し、その次に本書を優先する。

## 2. 作業原則

- 既存の言語、フレームワーク、ディレクトリ構成、命名、テスト方式を確認してから変更する。
- 依頼達成に必要な最小限の変更を行い、無関係なリファクタリングを混ぜない。
- 大会規則を暗黙に補完しない。未確定の事項は設定可能にするか、明示的なバリデーションエラーにする。
- スケジュールが生成できない場合、制約を黙って緩和しない。満たせなかった制約と原因候補を返す。
- ランダムな抽選や同順位の並び替えには `random_seed` を使用し、同じ入力と同じシードから同じ結果を再現できるようにする。
- 内部の正本はJSON互換データとし、HTML、表計算、PDFなどはそこから派生させる。
- 日時計算は文字列連結で行わず、日付・時刻または分単位の値として計算する。
- 内部IDと表示名を分離する。内部IDはコートや時刻の変更で変化しない意味ベースの値にする。
- 新しい動作には、正常系、境界値、実行不能系のテストを追加する。

### 2.1 言語

- エンドユーザーへ表示するメッセージ、エラー、警告、ヘルプは、原則として日本語にする。
- `README` を含むドキュメントは、原則として日本語で作成・更新する。
- 既存の文書やメッセージを編集する場合は、明確な理由または指示がない限り、そこで使われている言語を維持する。
- ユーザーから別の言語を明示的に指定された場合や、外部仕様との互換性のために言語が定められている場合は、その指定を優先する。

### 2.2 対象利用者とUX

- 主なエンドユーザーは地域大会の運営者、審判、チーム関係者とし、ITリテラシーを低く見積もって設計する。
- 主要な操作は、専門知識やマニュアルを前提とせず、画面の案内だけで完了できるようにする。
- 入力は段階的なウィザード、妥当な既定値、入力例、即時バリデーションを用い、一度に多数の設定項目を提示しない。
- 内部ID、JSON、ソルバーの状態名、スタックトレースなどの実装詳細を通常の画面へ直接表示しない。エラーや警告は、原因、影響、利用者が取るべき対応を日本語で具体的に示す。
- 入力内容と生成結果は自動保存し、誤操作、画面再読み込み、通信切断から復元できるようにする。破壊的な操作には確認と、可能な限り取り消し手段を設ける。
- 日程表、対戦表、審判割当て、チーム別予定の閲覧と印刷を主要な利用経路として扱い、スマートフォン、タブレット、PCで利用できるレスポンシブな画面にする。
- 会場で通信が不安定になる可能性を考慮し、保存済みの大会データと生成済みの日程は、可能な範囲でオフラインでも閲覧できるようにする。

### 2.3 提供形態とインフラ

- エンドユーザーによる実行環境、ランタイム、依存パッケージ、データベースなどのインストールを必要としないWebアプリとして提供する。
- フロントエンドは静的ホスティング、計算処理はスケール・トゥ・ゼロ可能なサーバーレス実行環境を第一候補とし、常時稼働するサーバーを初期構成の必須要件にしない。
- GitHub Pagesやクラウドサービスの無料枠または低料金枠で運用できる構成を優先し、固定費と保守対象を最小化する。ただし、費用削減のために大会規則、ハード制約、検証、セキュリティを弱めてはならない。
- 初期構成では、必要性が確認できないクラウドデータベースや利用者アカウントを導入しない。ブラウザ内の自動保存と、JSON互換の大会データの保存・読込みによって復元可能にする。
- クラウドへ送信するデータと保存するデータを最小化し、入力や生成結果を永続保存する場合は、保存目的、保存期間、アクセス制御、削除方法を先に定義する。
- サーバーレス計算には、入力サイズ、参加数、実行時間、同時実行数、再試行回数の上限を設ける。無料枠超過や意図しない課金を検知できる予算通知と利用量監視を用意する。
- 公開APIでは、CORSを認証や濫用防止の代替として扱わない。必要に応じて認証、レート制限、Bot対策を導入し、第三者による計算資源の濫用を抑止する。
- ソルバーやバックエンド固有の処理はフロントエンドから分離し、静的ホスティングやサーバーレス基盤を将来変更しても、大会規則とJSON互換データモデルを再利用できる構造にする。

## 3. 大会の全体像

- 大会は2日間開催する。
- 1日目は複数ブロックによる1回総当たりのリーグ戦とする。
- 2日目は1日目のブロック順位に基づき、上位トーナメントと下位トーナメントを行う。
- 両トーナメントは、勝者側だけでなく敗者側も次の順位決定戦へ進む「完全順位決定トーナメント」とする。
- 3位決定戦を行う。参加数に応じ、5位、7位などの順位決定戦も生成する。
- トーナメント参加数が2のべき乗でない場合も、シードと不戦通過相当の予備戦免除を用いて処理する。

## 4. 必須入力

最低限、次の情報を受け取る。

```yaml
teams:
  - id: team-01
    name: Team 1
    seed: 1                 # 任意。ブロック分けに利用可能

courts:
  - id: court-a
    name: Aコート

league:
  block_count: 4
  assignment_mode: random  # random | seeded_snake | manual
  odd_split_policy: upper  # upper | lower | alternate

days:
  day1:
    start_time: "09:30"
    game_duration_minutes: 35
    margin_minutes: 5
    max_sections: null      # 終了時刻を使う場合はどちらか一方でよい
    end_time: null
    breaks: []              # 例: [{after_section: 4, duration_minutes: 60}]
  day2:
    start_time: "09:30"
    game_duration_minutes: 35
    margin_minutes: 10
    max_sections: null
    end_time: null
    breaks: []

referees:
  organizer_capacity: 3
  tournament_fallback: organizer  # organizer | strict

tournament:
  placement_mode: full
  seeding:
    enabled: true
    basis: block_rank
    equal_rank_order: draw
    bye_policy: higher_seed
    avoid_same_block_first_match: true
    delay_same_block_rematch: true
  preliminary_round:
    enabled: true
    complete_before_main_round: true

random_seed: 20260803
```

### 4.1 入力バリデーション

- チームID、コートIDは大会内で一意かつ空でないこと。
- チーム数 `T >= 2`、コート数 `C >= 1`、ブロック数 `1 <= B <= T` とする。
- 各日のゲーム時間は正、マージンは0以上とする。
- `end_time` と `max_sections` の双方が未指定なら、必要セクション数を最小化して終了時刻を算出する。
- 双方が指定されて矛盾する場合はエラーにする。
- 主催者審判能力は0以上とする。未指定時の既定値はコート数 `C` とする。
- 手動ブロック分けの指定済みチームは重複なく1ブロックだけに所属すること。未割当ては入力途中として許可し、日程生成時に`random_seed`で自動補完した後は、全チームがちょうど1ブロックに所属すること。
- リーグ結果を反映して2日目を確定するときは、全リーグ試合の結果が入力済みであること。

## 5. 時刻、セクション、スロット

休憩がない場合、第 `s` セクションの開始時刻は次式で求める。

```text
section_start(s) = start_time + (s - 1) * (game_duration + margin)
```

休憩は指定されたセクションの終了後に加算する。表示上は、試合時間、マージン、セクション全体を区別する。

各セクションはコート数と同じ数のスロットを持つ。未使用スロットを許可する。スロットは少なくとも次を保持する。

```text
day_id
section_no
court_id
match_id | null
referee_assignment
```

不戦通過は実試合ではないため、スロット、コート、審判、得点、休憩判定を消費しない。

## 6. 1日目：リーグ戦

### 6.1 ブロック分け

`q = T div B`、`r = T mod B` とし、`r` ブロックを `q + 1` チーム、残りを `q` チームにする。ブロック間の人数差は最大1とする。

対応する分け方は次の3種類とする。

- `random`: `random_seed` を使ってシャッフルし、均等に割り当てる。
- `seeded_snake`: シード順をブロック間にスネーク方式で分散する。
- `manual`: 入力済みの所属を固定し、未割当てチームだけを`random_seed`で再現可能にシャッフルして均等な人数になるよう自動補完する。手動指定だけで均等化不能な人数超過がある場合は、指定済み所属を動かさず生成を拒否する。

手動方式の入力と結果を区別し、入力には利用者が指定した所属だけを保存する。生成結果には補完後の全所属と、自動配置したチーム・ブロックの監査情報を保存する。

各ブロック内で1回総当たりを行う。ブロック `b` の人数を `n_b` としたリーグ総試合数は、`sum(n_b * (n_b - 1) / 2)` である。対戦生成にはサークル法を利用できるが、論理上のラウンドと実際のセクションは別データとして扱う。

### 6.2 勝点と集計

- 勝利: 3点
- 引き分け: 1点
- 敗戦: 0点
- 勝点: `3 * wins + draws`
- 得失点差: `goals_for - goals_against`

リーグ戦ではPK戦を行わず、引き分けを認める。

### 6.3 ブロック内順位

次の順で比較する。

1. 全試合の勝点（降順）
2. 全試合の得失点差（降順）
3. 全試合の総得点（降順）
4. 直接対戦の成績
5. 抽選

直接対戦は、上記1〜3がすべて同じチーム群にのみ適用する。

- 2チーム同点の場合は両者の直接対戦の勝敗で決め、引き分けなら抽選とする。
- 3チーム以上同点の場合は、その同点グループ内の試合だけでミニリーグを作り、直接対戦内の勝点、得失点差、総得点の順で比較する。
- ミニリーグ比較後に一部だけが同点で残っても、残ったチームだけでミニリーグを作り直さない。最初の同点グループの値を維持し、それでも同じなら抽選とする。
- 抽選結果は監査できるよう、`random_seed`、候補、確定順を結果データに残す。

## 7. 上位・下位トーナメントへの振り分け

各ブロックの順位を上半分と下半分に分ける。

- 偶数人数 `n` のブロック: 上位 `n / 2`、下位 `n / 2`
- 奇数人数で `upper`: 上位 `ceil(n / 2)`、下位 `floor(n / 2)`
- 奇数人数で `lower`: 上位 `floor(n / 2)`、下位 `ceil(n / 2)`
- 奇数人数で `alternate`: 奇数人数のブロックを安定したブロック順に並べ、`upper` と `lower` を交互に適用する

既定値は `upper` とする。振り分け規則は大会全体で固定し、結果を見てから変更してはならない。

## 8. 2日目：完全順位決定トーナメント

### 8.1 勝敗

- 規定時間内の得点が多いチームを勝者とする。
- 同得点なら延長戦を行わず、直ちに各チーム3名のPK戦を行う。
- 3名終了時点で同数なら、以後1名ずつのサドンデスとする。
- 通常得点とPK得点を別フィールドに保存する。
- 選手登録を扱わない初期実装では、キッカー個人の管理は対象外とし、PK得点と勝者のみを記録する。

```yaml
regular_score_home: 1
regular_score_away: 1
penalty_score_home: 4
penalty_score_away: 3
winner: home
decision: penalty_shootout
```

通常得点が同点でないのにPK結果がある、通常得点が同点なのに勝者もPK結果もない、PK得点が同数のまま確定している、といった矛盾は拒否する。

### 8.2 シード順

異なるブロック間の得失点差や総得点は、対戦相手が異なるためシード比較に使わない。

1. ブロック内順位が高いチームを上位シード群にする。
2. 同じブロック順位同士は、再現可能な抽選で順序を決める。
3. 確定した順序を `seed_no` として保存する。

組合せの優先順位は次のとおりとする。

1. 不戦通過相当の予備戦免除を上位シードへ与える。
2. 予備戦では上位側のシードと下位側のシードを対戦させる。
3. 初戦の同一ブロック対戦を避ける。
4. 同一ブロック同士が再戦し得るラウンドをできるだけ遅らせる。
5. 以上が同等の候補は `random_seed` による抽選で決める。

同一ブロック回避が不可能な場合、組合せ生成自体を失敗させず、初戦の同一ブロック対戦数を最小化し、次に再戦可能ラウンドを最も遅くする。回避できなかった組合せと最適化評価値を警告として残す。

### 8.3 任意参加数の順位決定アルゴリズム

トーナメント参加数 `N` が2のべき乗である場合、各ラウンドの勝者群を上位半分、敗者群を下位半分の順位決定へ再帰的に送り、1位から `N` 位までを確定する。`N = 8` なら、1位、3位、5位、7位の各決定戦を含む12試合となる。

`N` が2のべき乗でない場合は、次の手順を使う。

```text
K = 2 ^ floor(log2(N))
R = N - K
予備戦数 = R
予備戦参加数 = 2R
予備戦免除数 = N - 2R = 2K - N
```

- 上位 `N - 2R` シードは予備戦を免除する。
- 下位 `2R` シードを上位対下位になるように組ませ、`R` 試合の予備戦を行う。
- 予備戦免除チームと予備戦勝者の計 `K` チームで上位側の完全順位決定を行う。
- 予備戦敗者 `R` チームでも同じアルゴリズムを再帰適用し、`K + 1` 位から `N` 位を決める。
- 結果未確定でも表を生成できるよう、予備戦勝者・敗者は参照型で保持し、由来となるシード番号を組合せ最適化に使用する。
- 原則として全予備戦を終えてから本戦の実試合を開始する。

6チームなら、第3対第6、第4対第5を予備戦とし、第1・第2シードは免除する。予備戦勝者4チームで1〜4位、予備戦敗者2チームで5〜6位を決めるため、計7試合となる。

### 8.4 不戦通過の表現

予備戦免除は試合結果の勝利として集計せず、`advance_by_bye` として記録する。

```yaml
entry: {type: league_rank, block_id: A, rank: 1}
result: advance_by_bye
next_match_id: UT-SF1
```

## 9. 内部データモデル

最低限、次の参照型を区別する。

```text
ConcreteTeam(team_id)
LeagueRank(block_id, rank)
WinnerOf(match_id)
LoserOf(match_id)
Organizer
```

試合は少なくとも次を持つ。

```yaml
id: UT-SF1
phase: upper_tournament
round: semifinal
home: {type: winner_of, match_id: UT-QF1}
away: {type: winner_of, match_id: UT-QF2}
rank_range: [1, 4]
```

IDの接頭辞は、例えばリーグ `LG`、上位 `UT`、下位 `LT` を用いる。`UT-QF1`、`UT-SF1`、`UT-FINAL`、`UT-PLACE3` のような意味ベースのIDを優先し、「Aコート第2試合」のような配置依存IDを使わない。

## 10. スケジュールのハード制約

以下は必ず満たす。満たせないときはスケジュールを返さず、診断情報を返す。

1. 1つの実試合は、ちょうど1つの日、セクション、コートへ割り当てる。
2. 1コートの1セクションには最大1試合を割り当てる。
3. 同一セクションで、同じチームを試合またはチーム審判へ2回以上割り当てない。
4. チーム審判の試合では、対戦2チームと審判チームをすべて異なるものにする。
5. 第 `s` セクションで試合をした可能性があるチームは、第 `s + 1` セクションで試合をしてはならない。
6. 勝者・敗者が後続試合へ進む場合、先行試合と後続試合の間に少なくとも1つの完全な休憩セクションを置く。すなわち先行が `s` なら後続は `s + 2` 以降とする。
7. トーナメントの未確定参照について、起こり得るすべての勝敗経路で制約3〜6を満たす。
8. 同一試合の前提となる全試合を、その試合より前に配置する。
9. 日の第1セクションにある全試合は主催者審判とする。
10. 上位・下位トーナメントの決勝は主催者審判とする。
11. 同一セクションの主催者審判数は `organizer_capacity` 以下とする。
12. `end_time` または `max_sections` が指定されている場合、その範囲を超えない。
13. 1日目は、同じチームが連続するセクションで試合またはチーム審判を担当する場合、試合→審判、審判→試合、審判→審判のいずれも同じコートに配置する。セクション間に設定休憩があっても連続とみなし、主催者審判は対象外とする。
14. 2日目は上位トーナメント決勝を、実試合がある最後のセクションへ配置する。上位決勝がなく下位決勝だけがある場合は、下位決勝を最後の実試合セクションへ配置する。決勝は外部注記ではなく `phase` と `rank_range: [1, 2]` から判定する。

不戦通過は制約5〜8における実試合として数えない。ただし不戦通過元の参照は正しく後続試合へ接続する。

## 11. 審判割当て

### 11.1 リーグ戦

- 日の第1セクションは主催者が担当する。
- それ以外は原則として参加チームを割り当てる。
- 審判回数の最大値と最小値の差を最小化し、可能なら差を1以内にする。
- 同点の候補では、同じコートの直前の試合に参加したチームを優先する。
- 審判直後の試合、空きセクションを挟む過度なコート移動、同一ブロックの試合だけを繰り返し担当する状態はソフト制約として避ける。連続セクションの担当はハード制約により同一コートとする。

### 11.2 トーナメント

- 日の第1セクションと、上位・下位の決勝は主催者が担当する。
- その他は原則として、そのコートで直前に行われた実試合の勝者が担当する。「直前」は直前セクションではなく、そのコート上の直前の実試合を意味する。
- 不戦通過を審判供給源にしてはならない。
- 直前試合の勝者が次の試合に出場し得る、同一セクションの別役割と衝突する、または直前の実試合がない場合、`tournament_fallback: organizer` なら主催者へ切り替える。
- `tournament_fallback: strict` なら配置を組み直し、実行可能解がなければエラーにする。
- 主催者への切替理由を結果データへ記録する。

## 12. ソフト制約と最適化順序

ハード制約を満たす解の中で辞書式に最適化し、後順位の改善のために前順位を悪化させない。1日目は次の順序とする。

1. 使用セクション数を最小化する。
2. リーグ戦の審判回数の最大差を最小化する。
3. 各チームの試合間の最大待ち時間を抑える。
4. 審判直後の試合を減らす。
5. リーグ戦で、直前に同じコートで試合したチームによる審判を増やす。
6. 空きセクションを挟むチームのコート移動回数を減らす。1日目の連続セクション間の移動はハード制約で禁止し、2日目専用モデルでは従来どおりソフト制約として扱う。
7. コート使用数の偏りを減らす。

2日目専用モデルは次の順序とする。

1. 使用セクション数を最小化する。
2. 下位トーナメント決勝と最後の実試合セクションとの差を最小化する。
3. 各チームの試合間の最大待ち時間を抑える。
4. チームのコート移動回数を減らす。
5. コート使用数の偏りを減らす。

最適解の証明が困難なソルバーを用いる場合は、タイムアウト、得られた評価値、最適性が証明済みかを出力する。

## 13. 出力

少なくとも次をJSONの正本に含め、必要に応じて人間向け形式へ変換する。

- 入力設定とスキーマバージョン
- リーグのブロック分けと全対戦
- 1日目のセクション別・コート別日程
- リーグ結果、順位表、各タイブレーク値、抽選記録
- 上位・下位のトーナメント表とシード根拠
- 2日目のセクション別・コート別日程
- チーム別の試合・審判予定
- 審判回数と主催者審判の使用数
- 各チームの試合間隔
- 未使用スロットと終了予定時刻
- ハード制約検証結果、警告、フォールバック理由

PK戦は、例えば `Team A 1 (PK 4-3) 1 Team B` と表示する。内部データでは通常得点とPK得点を結合しない。

## 14. エラーと診断

エラーには、機械可読なコードと日本語で理解できる説明を含める。少なくとも次を区別する。

- 入力スキーマ不正
- リーグ結果不足または矛盾
- トーナメント結果不正
- コートまたは利用可能時間の不足
- 休憩制約による配置不能
- 審判供給不足または主催者能力不足
- トーナメント依存関係の循環
- 指定時間内に実行可能解が見つからない

「解なし」と「探索タイムアウトで未発見」を同じ扱いにしない。診断では、必要試合数、利用可能スロット数、理論上の最低セクション数など、確認できた根拠を示す。

## 15. テスト要件

### 15.1 単体テスト

- 均等ブロック分け: `T mod B` が0の場合と非0の場合
- 総当たり生成: 奇数・偶数人数で重複、自己対戦、欠落がないこと
- 順位計算: 勝点、得失点差、総得点、2チーム直接対戦、3チーム以上のミニリーグ、抽選
- 上下分割: 偶数、`upper`、`lower`、`alternate`
- 完全順位決定表: 2、4、8チーム
- シード付き順位決定表: 3、5、6、7、9、10チーム
- PK結果の整合性検証
- 休憩とセクション時刻計算
- 未確定の勝敗参照を含む衝突判定
- 審判フォールバックと主催者能力上限

### 15.2 性質テスト

生成された大会表について、次を任意の妥当な参加数で検証する。

- 各リーグブロックの全ペアがちょうど1回対戦する。
- 上位・下位への所属が重複せず、全チームを覆う。
- トーナメントの全勝敗パターンで各参加チームの最終順位が一意に確定する。
- 1位から参加数までの順位に欠落と重複がない。
- すべての実試合が1回だけ配置される。
- ハード制約がすべて成立する。
- 同じ入力と `random_seed` から同一結果が得られる。

### 15.3 受入例

- 4ブロック各4チーム、上位8・下位8、3コートの場合、各トーナメント12試合、合計24試合の表を生成できること。
- 6チームの単一順位決定トーナメントは、予備戦2、1〜4位側4、5〜6位側1の計7試合になること。
- 予備戦が第1セクションなら、その勝者が参加し得る次戦は第3セクション以降になること。
- トーナメント同点試合で、延長戦を生成せず3人制PK入力を要求すること。

## 16. 実装完了条件

変更を完了と報告する前に、次を確認する。

- 本書の対象となる大会規則を実装し、規則を黙って変更していない。
- 追加・変更した動作に対応するテストがある。
- テスト、型検査、静的解析、ビルドのうちプロジェクトで利用可能なものを実行し、結果を報告した。
- 生成結果に対して独立した制約検証を実行した。
- ランダム処理が再現可能で、抽選記録が残る。
- 未解決の制約、未検証範囲、フォールバック発生を明示した。
- 秘密情報、個人情報、環境固有の絶対パスを成果物へ追加していない。

## 17. 現時点で仕様外とする事項

次は追加指示があるまで実装必須としない。

- 選手登録、出場資格、交代、警告・退場の管理
- PKキッカー個人とキック順の管理
- 天候、遅延、棄権、没収試合による当日再編成
- 複数会場間の移動時間
- 異なるブロック間の成績によるシード比較
- 外部大会規則への自動準拠

これらが必要になった場合は、既存の結果データとの後方互換性、再スケジュール範囲、順位計算への影響を先に定義する。
